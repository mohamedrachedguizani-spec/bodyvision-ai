# nixpacks.toml — Configuration de build pour Railway (Nixpacks builder)
# Root Directory doit être défini sur "backend" dans Railway Settings
# IMPORTANT : NE PAS utiliser LD_LIBRARY_PATH — casse le runtime Nix (__vdso_gettimeofday)
# Solution : sitecustomize.py charge libGL via ctypes.CDLL avant tout import

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[phases.setup]
aptPkgs = [
  "curl",
  "libmagic1",
  "libgl1",
  "libglx0",
  "libglvnd0",
  "libglib2.0-0",
  "libsm6",
  "libxext6",
  "libxrender1",
  "libgomp1",
  "patchelf"
]

[phases.build]
cmds = [
  "pip uninstall -y opencv-python opencv-contrib-python || true",
  "pip install --force-reinstall opencv-python-headless==4.8.1.78",
  "/opt/venv/bin/python build_scripts/install_libgl_pth.py || true"
]

[start]
cmd = "sh startup.sh && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1 --timeout-keep-alive 120"
